# Cursor Rule: Two-Sided Verification Gate

Defines the `/verify` protocol: run the project's positive control AND a planted negative control that must fail naming the plant, with the tree restored unchanged. Wraps `adapters.py gate` so single-threaded work reaches the same gate an orchestrated lane gets, instead of claiming in prose that both controls passed.
